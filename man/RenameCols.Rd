% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renameCols.R
\name{RenameCols}
\alias{RenameCols}
\title{Interactively rename columns}
\usage{
RenameCols(
  x,
  NamesDF = TRUE,
  renameDF = TRUE,
  rename_map = NULL,
  quiet = FALSE
)
}
\arguments{
\item{x}{A list returned by \code{\link[=FlattenSubmissions]{FlattenSubmissions()}}.}

\item{NamesDF}{Logical. If \code{TRUE} (default), return a data frame of the old
and new names.}

\item{renameDF}{Logical. If \code{TRUE} (default), return the dataset with the
updated column names.}

\item{rename_map}{Optional. A mapping to apply without prompting. Accepted
forms:
\describe{
\item{named character vector}{Names are old column names, values are new column names.}
\item{data.frame}{With columns \code{OldNames} and \code{NewNames}. Extra columns are ignored.}
}
If provided, \code{RenameCols()} runs non-interactively and applies the mapping
to any matching columns.}

\item{quiet}{Logical. If \code{TRUE}, suppresses most printing (useful inside the
workflow wizard). Default \code{FALSE}.}
}
\value{
A list that may include:
\describe{
\item{renamedDF}{A data frame with columns \code{Number}, \code{OldNames}, \code{NewNames}.}
\item{flat}{The updated \code{\link[=FlattenSubmissions]{FlattenSubmissions()}} list with renamed columns.}
}
}
\description{
Guides you through renaming each column in a flattened FormIO dataset.
This is helpful for non-technical users who want friendly column names
before analysis or export.
}
\details{
If audit logging is active (see \code{\link[=StartAuditLog]{StartAuditLog()}}), this action is recorded.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
flat <- FlattenSubmissions(GetResponses(form_id = "123", api_key = "abc"))
RenameCols(flat)
\dontshow{\}) # examplesIf}
}
