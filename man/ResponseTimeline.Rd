% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AnalysisHelpers.R
\name{ResponseTimeline}
\alias{ResponseTimeline}
\title{Summarize responses over time}
\usage{
ResponseTimeline(
  x,
  date_col = NULL,
  interval = c("day", "week", "month", "hour"),
  tz = "UTC",
  start = NULL,
  end = NULL,
  include_empty = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A data frame of responses, or a list from \code{\link[=FlattenSubmissions]{FlattenSubmissions()}}.}

\item{date_col}{Column name or number for the date/time field. If \code{NULL},
tries to guess a reasonable timestamp column.}

\item{interval}{One of \code{"day"} (default), \code{"week"}, \code{"month"}, or \code{"hour"}.}

\item{tz}{Time zone to use for parsing dates (default \code{"UTC"}).}

\item{start}{Optional start date/time to filter the range.}

\item{end}{Optional end date/time to filter the range.}

\item{include_empty}{Logical. If \code{TRUE}, fill missing periods with zeroes.}

\item{quiet}{Logical. If \code{FALSE}, prints a short summary.}
}
\value{
A list with:
\describe{
\item{data}{Data frame with \code{period} and \code{count} columns}
\item{summary}{List with metadata about the calculation}
}
}
\description{
Counts responses per day/week/month (or hour), using a timestamp column.
If \code{date_col} is \code{NULL}, the function tries common timestamp names
automatically (e.g., \code{created}, \code{modified}, \verb{_createdAt}).
}
\examples{
\dontrun{
ResponseTimeline(flat, date_col = "created", interval = "week")
}
}
