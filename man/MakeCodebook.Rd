% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReportingExport.R
\name{MakeCodebook}
\alias{MakeCodebook}
\title{Create a codebook for a dataset}
\usage{
MakeCodebook(
  data,
  form = NULL,
  include = c("input", "all"),
  include_summary = TRUE,
  max_levels = 20,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame of responses, or a list containing \code{FlatResponses}
or \code{submission_data}.}

\item{form}{Optional form schema, JSON string, or metadata object (see
\code{\link[=FieldDictionary]{FieldDictionary()}}). If provided, labels and sections are included.}

\item{include}{Which components to include when \code{form} is provided:
\code{"input"} (default) or \code{"all"}.}

\item{include_summary}{Logical. If \code{TRUE} (default), include basic numeric
summaries (min, max, mean).}

\item{max_levels}{Maximum number of category levels to list for character or
factor columns.}

\item{quiet}{Logical. If \code{FALSE}, prints a short message.}
}
\value{
A data.frame codebook with one row per column.
}
\description{
This produces a plain-language table that documents each column in your data.
It can optionally use a FormIO schema (via \code{\link[=FieldDictionary]{FieldDictionary()}}) to add labels,
sections, and descriptions.
If audit logging is active (see \code{\link[=StartAuditLog]{StartAuditLog()}}), this action is recorded.
}
\details{
When a schema is provided, the codebook also includes a \code{schema_key} column
showing which form field key was matched (best-effort). If a column could not
be matched to the schema, schema-related columns (type/required/section/etc.)
will be \code{NA}.
}
\examples{
df <- data.frame(age = c(10, 12, NA), color = c("red", "blue", "red"))
MakeCodebook(df)
}
